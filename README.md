
  # Premium Diagnostics Homepage Prototype

  This is a code bundle for Premium Diagnostics Homepage Prototype. The original project is available at https://www.figma.com/design/vHEoJAsRtz3iN02y59LkaQ/Premium-Diagnostics-Homepage-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  